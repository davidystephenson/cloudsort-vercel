import { DeleteIcon } from '@chakra-ui/icons'
import { IconProps } from '@chakra-ui/react'

export default function RemoveIconView (props: IconProps): JSX.Element {
  return <DeleteIcon {...props} />
}
